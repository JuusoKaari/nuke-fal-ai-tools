# Local planning notes at repo root (not shipped).
# Anchor so docs/planning/ can still hold tracked specs.
/planning/
/TODO.md

# AGENTS.md is tracked so clones see contributor rules. The release ZIP still excludes it.

# Private unattended runner (lives in nuke-fal-ai-tools_devtools)
dev_tools/
unattended-todo.json

# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Cursor
.cursorignore
.cursor/

# Local Execute output
nuke_fal_output/
